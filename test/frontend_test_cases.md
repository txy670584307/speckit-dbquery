# 前端测试用例

---

## 模块一：数据库连接管理（DbList + 添加对话框）

### TC-UI-001：首次加载——空状态

| 字段 | 值 |
|------|----|
| **前置条件** | 无已保存的数据库连接 |
| **操作** | 打开 `http://localhost:8080` |
| **预期** | 左侧显示"暂无连接，点击"添加"开始"；右侧显示"请从左侧选择一个数据库连接" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-002：打开添加对话框

| 字段 | 值 |
|------|----|
| **前置条件** | 页面已加载 |
| **操作** | 点击左侧「添加」按钮 |
| **预期** | 弹出对话框，标题"添加数据库连接"，包含「连接名称」和「JDBC URL」输入框，底部「取消」和「连接」按钮 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-003：添加连接——表单验证（空字段）

| 字段 | 值 |
|------|----|
| **前置条件** | 添加对话框已打开 |
| **操作** | 不填任何内容，直接点击「连接」 |
| **预期** | 表单验证不通过，两个输入框下方显示错误提示（如"请输入连接名称"、"请输入 JDBC URL"） |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-004：添加连接——JDBC URL 格式验证

| 字段 | 值 |
|------|----|
| **前置条件** | 添加对话框已打开 |
| **操作** | 填写有效连接名，JDBC URL 输入 `mysql://...`，点击「连接」 |
| **预期** | URL 格式验证失败，提示"必须以 jdbc:postgresql:// 开头" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-005：添加连接——成功

| 字段 | 值 |
|------|----|
| **前置条件** | 添加对话框已打开，连接名 `testui` 不存在 |
| **操作** | 输入连接名 `testui`，JDBC URL 输入有效的 PostgreSQL 连接串，点击「连接」 |
| **预期** | 对话框关闭；左侧列表出现 `testui`；右侧显示该数据库的表/视图树；提示"连接 'testui' 添加成功" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-006：添加连接——后端错误提示

| 字段 | 值 |
|------|----|
| **前置条件** | 添加对话框已打开 |
| **操作** | 输入连接名 `fakedb`，JDBC URL 指向不存在的端口，点击「连接」 |
| **预期** | 对话框不关闭；顶部出现错误提示（如"数据库连接失败: 连接超时"） |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-007：切换数据库连接

| 字段 | 值 |
|------|----|
| **前置条件** | 至少有两个已保存连接 |
| **操作** | 点击左侧列表中的不同连接 |
| **预期** | 连接高亮切换；中间编辑器区域清空先前查询结果；右侧表/视图树更新为对应连接的 metadata |
| **实际结果** | |
| **状态** | ⬚ |

---

## 模块二：表/视图树（TableTree）

### TC-UI-008：树形结构展示

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接到 `testdb` |
| **操作** | 观察左侧表/视图树 |
| **预期** | 按 schema 分组（`public`、`sales`），每个 schema 下显示表名，表名可展开显示列名和数据类型 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-009：展开查看列信息

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接到 `testdb` |
| **操作** | 展开 `users` 表节点 |
| **预期** | 显示列：`id (integer)`、`username (character varying)`、`email (...)`、`full_name`、`is_active`、`created_at`，nullable 列旁有 `nullable` 标签 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-010：无表/视图提示

| 字段 | 值 |
|------|----|
| **前置条件** | 连接到一个空的数据库 |
| **操作** | 切换到该连接 |
| **预期** | 表区域显示"该数据库中没有表或视图" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-011：metadata 加载中状态

| 字段 | 值 |
|------|----|
| **前置条件** | 网络较慢 |
| **操作** | 点击一个连接 |
| **预期** | 表区域显示加载 spinner `⏳ 加载中...` |
| **实际结果** | |
| **状态** | ⬚ |

---

## 模块三：SQL 编辑器（SqlEditor）

### TC-UI-012：Monaco Editor 渲染

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接数据库 |
| **操作** | 查看右侧 SQL 编辑区域 |
| **预期** | 显示 Monaco Editor，带 SQL 语法高亮、行号、工具栏（含"按 Ctrl+Enter 执行"提示和「执行」按钮） |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-013：输入 SQL 并手动点击执行

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb` |
| **操作** | 在编辑器中输入 `SELECT * FROM users`，点击「执行」按钮 |
| **预期** | 按钮变为 loading 状态；下方 ResultTable 显示 users 表数据（5 行） |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-014：Ctrl+Enter 快捷键执行

| 字段 | 值 |
|------|----|
| **前置条件** | 同上 |
| **操作** | 编辑器中输入 SQL，按 `Ctrl+Enter` |
| **预期** | 等价于点击「执行」按钮，查询返回结果 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-015：SQL 语法错误提示

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb` |
| **操作** | 输入 `SELECCT * FORM users` 并执行 |
| **预期** | 弹出错误提示（如"SQL 语法错误"），结果区域清空 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-016：非 SELECT 语句拒绝

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb` |
| **操作** | 输入 `DELETE FROM users WHERE id = 1` 并执行 |
| **预期** | 弹出错误提示"仅支持 SELECT 查询" |
| **实际结果** | |
| **状态** | ⬚ |

---

## 模块四：查询结果表格（ResultTable）

### TC-UI-017：结果表格展示

| 字段 | 值 |
|------|----|
| **前置条件** | 执行了 `SELECT id, username, email FROM users` |
| **操作** | 观察结果区域 |
| **预期** | 顶部显示"查询结果：5 行"，表格显示三列：`id`、`username`、`email`，数据正确 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-018：空结果提示

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb` |
| **操作** | 执行 `SELECT * FROM users WHERE 1=0` |
| **预期** | 显示"查询未返回任何结果" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-019：LIMIT 截断提示

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb` |
| **操作** | 执行 `SELECT * FROM users`（无 LIMIT） |
| **预期** | 结果顶部显示黄色标签"已限制显示前 1000 行" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-020：初始无结果状态

| 字段 | 值 |
|------|----|
| **前置条件** | 刚加载页面，未执行任何查询 |
| **操作** | 观察右侧下方区域 |
| **预期** | 显示"执行查询后结果将在此显示" |
| **实际结果** | |
| **状态** | ⬚ |

---

## 模块五：模式切换

### TC-UI-021：SQL/自然语言模式切换

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接数据库 |
| **操作** | 点击顶部「自然语言」按钮 |
| **预期** | 编辑器区域替换为自然语言输入框（多行文本）和「生成 SQL 并查询」按钮 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-022：自然语言查询成功

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb`，Ollama 运行中 |
| **操作** | 切换到自然语言模式，输入"活跃用户有哪些"，点击「生成 SQL 并查询」 |
| **预期** | 按钮 loading → 下方 ResultTable 显示查询结果；如生成 SQL 无法执行，显示黄色警告框 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-023：自然语言模式——生成 SQL 修正

| 字段 | 值 |
|------|----|
| **前置条件** | 自然语言查询失败（生成的 SQL 无效） |
| **操作** | 观察黄色警告框中的 SQL，编辑后点击「执行修改后的 SQL」或「跳转到 SQL 编辑模式」 |
| **预期** | "执行修改后的 SQL" → 直接查询并显示结果；"跳转到 SQL 编辑模式" → 切换到 SQL 模式并预填 SQL |
| **实际结果** | |
| **状态** | ⬚ |

---

## 模块六：缓存刷新

### TC-UI-024：手动刷新 metadata

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接 `testdb`，表/视图树已加载 |
| **操作** | 点击表/视图区域右上角的「刷新」按钮 |
| **预期** | 按钮变为 loading 状态 → 完成后表/视图树重新加载，提示"metadata 已更新" |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-025：刷新失败保留旧缓存

| 字段 | 值 |
|------|----|
| **前置条件** | 已连接，PostgreSQL 容器已停止 |
| **操作** | 点击「刷新」 |
| **预期** | 提示"刷新失败，保留上次缓存的数据"，表/视图树保持上次的数据 |
| **实际结果** | |
| **状态** | ⬚ |

---

## 模块七：多连接与状态管理

### TC-UI-026：切换连接时清空查询结果

| 字段 | 值 |
|------|----|
| **前置条件** | 有两个已保存连接，当前连接有查询结果 |
| **操作** | 点击左侧另一个连接 |
| **预期** | 结果表格和编辑器内容被清空，右侧回到初始状态 |
| **实际结果** | |
| **状态** | ⬚ |

### TC-UI-027：页面刷新后保持连接

| 字段 | 值 |
|------|----|
| **前置条件** | 已有已保存连接 |
| **操作** | 刷新浏览器页面 |
| **预期** | 左侧连接列表重新加载，第一个连接自动选中，表/视图树显示 |
| **实际结果** | |
| **状态** | ⬚ |
